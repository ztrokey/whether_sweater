
language: ruby
rbenv:
- 2.7.2
before_install:
- gem install bundler
install: gem install rails
addons:
  postgresql: 9.6
script:
- bundle exec rails db:{create,migrate} RAILS_ENV=test
- bundle exec rspec
services:
- postgresql
run: rails db:migrate